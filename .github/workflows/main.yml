name: Demo

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # 拉代码
      - uses: actions/checkout@v6

      # 安装Java
      - uses: actions/setup-java@v6
        with:
          distribution: 'temurin'
          java-version: '17'

      # 执行命令
      - run: java -version
